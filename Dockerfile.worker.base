ARG CUDA_BASE=nvidia/cuda:12.9.1-cudnn-devel-ubuntu20.04@sha256:4d6d1f25e5d0d6df4b8d7b8eb4d253bb5ab6c0cab9e1be71126c99a8be3c1d0c
FROM ${CUDA_BASE} AS base

ARG DEBIAN_FRONTEND=noninteractive
WORKDIR /app

# Install Python 3.9 and pip
RUN apt-get update && \
    apt-get install -y software-properties-common && \
    add-apt-repository ppa:deadsnakes/ppa && \
    apt-get update && \
    apt-get install -y python3.9 python3.9-distutils python3.9-venv curl && \
    curl -sS https://bootstrap.pypa.io/get-pip.py | python3.9


# ---- Development image ----
FROM base AS dev
RUN pip install --no-cache-dir "watchdog[watchmedo]"

# ---- Production image ----
FROM base AS prod
# (no extra packages – lean runtime)